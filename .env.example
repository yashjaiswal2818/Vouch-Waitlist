# EchoWidget - Example environment variables
# Copy this file to .env.local and fill in real values for local development.
# Do NOT commit secrets in version control.

# Public Supabase URL for your project (safe to expose to browser)
# Example: https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT-ID.supabase.co"

# Public Supabase anon key (safe to expose to browser)
# Obtain from Supabase project settings -> API -> Project API keys
NEXT_PUBLIC_SUPABASE_ANON_KEY="supabase-anon-key"

# Supabase service role key (server-side only; powerful key)
# Use only in secure server environments (server functions, edge, backend).
# Never expose to client code or ship in frontend bundles.
SUPABASE_SERVICE_ROLE_KEY="supabase-service-role-key"

