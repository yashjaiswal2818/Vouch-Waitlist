# Vouch-Waitlist

Minimal waitlist app built with React + Vite.

## Development

```
npm install
npm run dev
```

## Build

```
npm run build
npm run preview
```

## Environment

Configure variables in `.env.local` (see `.env.example`).
